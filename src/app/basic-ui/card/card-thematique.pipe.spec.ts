import { CardThematiquePipe } from './card-thematique.pipe';

describe('CardThematiquePipe', () => {
  it('create an instance', () => {
    const pipe = new CardThematiquePipe();
    expect(pipe).toBeTruthy();
  });
});
