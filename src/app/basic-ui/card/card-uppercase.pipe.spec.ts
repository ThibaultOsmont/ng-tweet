import { CardUppercasePipe } from './card-uppercase.pipe';

describe('CardUppercasePipe', () => {
  it('create an instance', () => {
    const pipe = new CardUppercasePipe();
    expect(pipe).toBeTruthy();
  });
});
